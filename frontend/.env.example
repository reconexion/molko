VITE_API_URL=http://localhost:8000

# Testing-only escape hatch, keep "true". Set to "false" locally only while
# the backend also runs with REQUIRE_SUBSCRIPTION=false.
VITE_REQUIRE_SUBSCRIPTION=true
